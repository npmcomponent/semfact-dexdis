extends layout

block content
	- var pushclasses = 'dimmed-content'
	div.dimmed-content
		div.container
			ul.commands
				each command in commands
					li.command
						a(href=prefix+'/commands/'+command.name+'.html')
							div
								span.name= command.name
								span.text-muted= ' ' + command.usage
							span.descr= command.short_description
