{
	"usage": "keyinfo cond func left",
	"description": "Walk through the list keyinfo until cond and do func for each element. If left is true, start at the first element, if it's false, at the last element.",
	"dependencies": [
	],
	"command": "_lwalk.coffee"
}
